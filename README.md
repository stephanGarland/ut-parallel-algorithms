## UT Parallel Algorithms HW3

CPU version for comparison can be compiled on anything with gcc that supports C++11
`make cpu`

GPU version requires CUDA and Thrust
`make gpu`

Clean with `make clean`
